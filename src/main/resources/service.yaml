apiVersion: v1
kind: Service
metadata:
  name: bookappbackend
spec:
  type: NodePort
  selector:
    app: bookappbackend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 32000
